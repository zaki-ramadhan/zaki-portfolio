rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Siapapun bisa melihat gambar project
      allow read: if true;
      // Hanya Anda (user login) yang bisa upload/hapus gambar
      allow write: if request.auth != null;
    }
  }
}
